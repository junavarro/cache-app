import { InstructionGenerator } from './instruction-generator';

describe('InstructionGenerator', () => {
  it('should create an instance', () => {
    expect(new InstructionGenerator()).toBeTruthy();
  });
});
